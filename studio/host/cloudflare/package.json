{
  "name": "ppt-master-hosted-official-ui",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build_assets.mjs",
    "test:contract": "node test_official_confirm_ui_contract.mjs && node test_official_svg_editor_contract.mjs",
    "test": "npm run build && npm run test:contract && node test_hosted_official.mjs && node test_hosted_editor.mjs",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@xmldom/xmldom": "^0.8.10"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
